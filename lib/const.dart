enum kOrientation{
  vertical,
  horizontal
}

enum kWallSize {
  two,
  three
}

// Note: Order in kDirection important
// TODO ordered enum in dart?
enum kDirection {
  forward,
  down,
  backward,
  up
}

enum kGameObjects {
  cheese,
  blueCheese,
  trap
}

enum kViews {
  homeView,
  gameView,
  gameOverView,
}